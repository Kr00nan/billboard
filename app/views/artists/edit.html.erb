<p>Hey! You've reached the EDIT page for Artists!<p>

<%= form_for @artist do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :genre %>
  <%= f.text_field :genre %>

  <%= f.submit %>
<% end %>

<button><%= link_to 'Back', artists_path %></button>
<button><%= link_to 'Edit', edit_artist_path(artist) %></button>
